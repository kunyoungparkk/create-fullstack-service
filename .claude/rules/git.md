---
description: Git 커밋·브랜치·PR 작성법을 설명한다.
---

# 제목

- Conventional Commits은 사용하지 않는다.
- 50자를 넘지 않는다.
- 마침표로 끝맺지 않는다.
- 명사형으로 종결한다.
- 식별자는 표기 그대로 쓰고, 여럿이면 쉼표로 구분한다.
- `(#숫자)`는 PR 반영 시 자동 부여되니 직접 쓰지 않는다.

## 본문

- 한 줄은 72자를 넘지 않는다. URL, `#` 로 시작하는 라인은 예외로 한다.
- 제목으로 의미가 충분히 전달되면 본문은 생략한다.
- 하십시오체로 변경 사항이나 이유를 짧게 적는다.
- 어떻게 코드를 작성했는지보다 무엇을, 왜 변경했는지를 적는다.

## 예시

```
Sheet 구현
```

```
Input, Textarea 구현
```

```
Toast 버그 수정
```

```
i18n 설정
```

```
의존성 패키지 업데이트
```

```
AvatarsModule을 UsersModule로 변경
```

```
ProfileInfo 개선

로딩 시 스켈레톤을 보여줍니다.
```

```
AuthService 개선

OTP 인증 시 적절한 예외 발생시킵니다.
```

```
User 수정

User에서 국가를 삭제하고 닉네임을 이름으로 변경합니다.
```

# 브랜치

- `main`: 출시 가능 상태
- `dev`: 다음 배포 개발 코드 (PR의 **기본 머지 대상**)
- 보조: `feature/*`, `fix/*`

**보조 브랜치 형식**: `<타입>/<이슈번호>-<기능명>`

- 예: `feature/42-user-authentication`
- `<이슈번호>`: 착수한 GitHub 이슈 번호 (필수, 이슈 없이 분기 금지)
- `<기능명>`: **kebab-case**, 3~5 단어 이내

# PR

- 제목 접두어 `[FEAT]` `[FIX]` 등 + 핵심 내용 (예: `[FEAT] 소셜 로그인 구현`)
- 본문은 `.github/pull_request_template.md` 템플릿을 따라 **한글**로 작성
- PR 템플릿 `## 관련 이슈`에 이슈 링크, 커밋/PR 본문에 `#<번호>` 참조
- 머지 대상은 `dev` (출시 시점에만 `dev` → `main`)
